# 禁止直接访问私钥/数据/配置目录
<IfModule mod_authz_core.c>
  <FilesMatch "\.(pem|lock|log)$">
    Require all denied
  </FilesMatch>
</IfModule>
<IfModule !mod_authz_core.c>
  <FilesMatch "\.(pem|lock|log)$">
    Order allow,deny
    Deny from all
  </FilesMatch>
</IfModule>
